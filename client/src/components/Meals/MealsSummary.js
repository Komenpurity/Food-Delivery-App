import classes from "./MealsSummary.module.css";

const MealsSummary = () => {
  return (
    <section className={classes.summary}>
      <i className="bi bi-quote">Healthy foods delivered at the comfort of your home</i>
    </section>
  );
};

export default MealsSummary;
